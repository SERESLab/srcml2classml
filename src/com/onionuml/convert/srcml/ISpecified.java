package com.onionuml.convert.srcml;

/**
 * Specifies an object that can receive specifier strings.
 */
public interface ISpecified {

	/**
	 * Adds the given specifier string to this object.
	 */
	public void addSpecifier(String specifier);
}
